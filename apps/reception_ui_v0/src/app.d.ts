// 本アプリはDB非接続（ダミー結合）。Locals に Supabase は持たない。
declare global {
  namespace App {
    // interface Error {}
    // interface Locals {}
    // interface PageData {}
    // interface Platform {}
  }
}

export {};
