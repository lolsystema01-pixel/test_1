{
  "name": "reception-ui-v0",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "description": "Web受付UI v0.4（T2 再配達・荷受人対応／不在票QR・URLから／問合番号+OTP認証→受付→完了。検証環境・ダミー・本番DB非接続）",
  "scripts": {
    "dev": "vite dev",
    "build": "vite build",
    "preview": "vite preview",
    "check": "svelte-kit sync && svelte-check --tsconfig ./tsconfig.json",
    "test": "node --import tsx --test test/*.test.ts"
  },
  "devDependencies": {
    "@sveltejs/adapter-auto": "^3.2.0",
    "@sveltejs/kit": "^2.5.0",
    "@sveltejs/vite-plugin-svelte": "^4.0.0",
    "@types/node": "^20.19.43",
    "svelte": "^5.0.0",
    "svelte-check": "^4.0.0",
    "tsx": "^4.19.0",
    "typescript": "^5.5.0",
    "vite": "^5.4.0"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.45.0"
  }
}
