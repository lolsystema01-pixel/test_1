# 確認結果メモ（配車サマリ v0.2）

- 実施日: 2026-07-24
- 事前検証: Claude Code（pglite **16/16 PASS**）
- 実機・共有Supabaseでの適用: **未実施**（SQL Editor への適用待ち＝【人】作業）
- 種別: 配車結果の集計ビュー（deliveries ベース・3指標＋ドライバー別＋明細）＋seed＋check

## 成果物
- `dispatch_summary_v0.sql`（dispatch_summary／dispatch_summary_by_driver／dispatch_summary_detail・全て security_invoker=on）
- `seed_dispatch_summary_v0.sql`（仮割当5／保留2／希望外3・冪等）
- `check_dispatch_summary_v0.sql`（3指標・概況カード一致・保留定義・ドライバー別・RLSなりすまし）
- `pglite_test.mjs`（16/16）

## 事前検証（pglite 16/16）

| 観点 | 内容 | 判定 |
|---|---|---|
| 3指標 | A01/当日：仮割当4(件)/1(人)・保留2・希望外3・受信16 | ☒ |
| 仮割当の定義 | `driver_id LIKE '仮%'`＝office_home_summary（概況カード）と同一定義で一致 | ☒ |
| 保留 | `common_id IS NULL かつ status='保留'` で拾う | ☒ |
| **希望外(#28同一条件)** | 実のみ・DRV2(希望{ZA}→ZB)=3・DRV3(希望NULL)=0(数えない)・仮=0 | ☒ |
| 明細カテゴリ | 仮割当4/保留2/希望外3/正常7（排他・順序判定） | ☒ |
| **RLS範囲外0件** | area A01=自営業所のみ(受信16)・C01は0／area C01=C01のみ(受信3)・A01は0（範囲内>0と対） | ☒ |
| 希望外フォールバック | preferred_areas 全NULL→希望外0（#28/シフトv0.7 実装前の状態） | ☒ |

## 合格条件との対応
- 対象日×自営業所で3件数が出る・0でない指標が分かる … ✅（①3指標・画面の警告色はフロント）
- 仮割当の件数・人数が概況カード(office_home_summary 仮配車)と一致 … ✅（同一定義・pglite/checkで実証）
- 保留が「common_id未付与かつ保留」で拾われる … ✅（定義一致）
- ドライバー別内訳（担当件数・仮か・希望外件数）＋3指標の明細一覧 … ✅（by_driver・detail）
- area ログインで自営業所のみ・範囲外0件 … ✅（RLS security_invoker）
- 希望外は #28/シフトv0.7 未実装の間0・実装後に件数 … ✅（フォールバック実証・seed で点灯も実証）
- サマリから再実行を起動・既存割当クリアの確認 … ⏸ **フロント担当（画面＋dispatch_build 呼出）**。本モジュールはDB（読取集計）まで。

## 申し送り（実機・次担当）
- **画面はフロント担当**（3指標＋ドライバー別＋明細＋警告色＋再実行導線・確認ダイアログ）。本モジュールはビュー3本まで。
- **希望外の点灯条件**: preferred_areas にデータ＋#28（`assign_priority/v0_3`）マージ。それまで実運用では0（ビューは正しいので自動点灯）。
- **security_invoker 依存**: ビューは deliveries と work_schedules を読む＝呼出ロールに両方の SELECT 権＋RLS が要る（実Supabaseは付与済み）。
- 表示名が要れば画面側で #28 `common_id_display` を join（#28 マージ後）。
