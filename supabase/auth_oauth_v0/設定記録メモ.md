# 設定記録メモ（認証 v0.3 / 【人】GUI設定）

⚠️ **クライアントシークレット等の秘密情報の「値」は、このメモに書かないこと。**
ここに書くのは「設定したか（済/未）」と「環境変数名」だけ。

- 実施日: 2026 / 06 / 11
- 実施者: 蜂谷

## Google Cloud Console

- [✓] GCPプロジェクト用意（プロジェクト名/ID: LOLシステム_test ）
- [✓] OAuth同意画面 設定（テストユーザーに自分のアカウント追加）
- [✓] OAuth 2.0 クライアントID 作成（種類：ウェブアプリケーション）
- [✓] 承認済みリダイレクトURI に Supabase の Callback URL を登録
- クライアントID（※秘密ではない＝公開前提。OAuthのURLに出る）: ☐ 控え済み
- クライアントシークレット: ☐ 安全に保管済み（**値は書かない**）

## Supabase ダッシュボード

- [✓] Authentication → Providers → Google を Enable
- [✓] クライアントID／シークレットを入力
- [✓] Callback URL をコピーして Google 側に登録（往復完了）
- Supabase Callback URL（公開情報なので可）: https://biriipdkbjuaaiwzgysz.supabase.co/auth/v1/callback

## 環境変数（任意・参考 / 名前のみ・値は書かない）

> ℹ️ **ホスティング版Supabaseで運用する今は、この欄は任意（参考）。**
> ID／シークレットはダッシュボードに入力した時点で **Supabase側に保存**される
> （シークレットは暗号化保管）。ログインだけならこの環境変数は不要。
> 必要になるのは「ローカルのSupabase CLI(config.toml)」や「Cloud Run等から扱う」場合。

| 環境変数名 | 用途 | 設定済み |
|-----------|------|---------|
| `SUPABASE_AUTH_GOOGLE_CLIENT_ID` | GoogleクライアントID | ☐（任意） |
| `SUPABASE_AUTH_GOOGLE_SECRET` | Googleクライアントシークレット | ☐（任意） |

## 備考

（同意画面のUser Type、テストユーザー、ハマり所など）
