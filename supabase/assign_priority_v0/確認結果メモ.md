# 確認結果メモ（配車 割当優先順位 v0.3）

- 実施日: 2026-07-24
- 事前検証: Claude Code（pglite **21/21 PASS**）
- 実機・共有Supabaseでの適用: **未実施**（SQL Editor への適用待ち＝【人】作業）
- 種別: 配車 dispatch_build の希望エリア第一（最小改修）＋表示名ビュー＋希望外記録

## 成果物
- `offices_preferred_area_first_v0.sql`（offices.preferred_area_first・既定false・dispatch_priorityと別軸）
- `dispatch_build_preferred_v0.sql`（cap_wire版を完全転記＋Phase1 order by 1句差込＋offices読取＋off_preference/mode記録・骨格無変更）
- `common_id_display_view_v0.sql`（common_id→area・municipality＋min〜max zone_no・area_master由来）
- `pglite_test.mjs`（21/21）

## 事前検証（pglite 21/21）

| 観点 | 内容 | 判定 |
|---|---|---|
| 列追加 | offices.preferred_area_first が boolean・既定 false | ☒ |
| **回帰一致** | **false の割当が改修前(cap_wire版)と driver×common_id×件数で完全一致**（実データ突合） | ☒ |
| cap 回帰 | DRV1=100/DRV2=60（shift_labels 結線維持・cap式#27無変更） | ☒ |
| false=残荷量最大 | DRV1(cap大・先)→CA(残100)、DRV2→CB(残60) | ☒ |
| **true=希望第一** | DRV1(希望{CB})が残荷量最大の CA より CB を先取り（Phase1 order by 差込が効く） | ☒ |
| フォールバック | DRV2(希望NULL)は true でも残荷量最大の CA（現行挙動に自然復帰） | ☒ |
| **希望外が減る** | 希望外(off_preference件数) false:100 → true:0 | ☒ |
| 未割当は増えない | 両モードとも未割当0（true では CA 余剰40を仮ドライバーが吸収） | ☒ |
| 希望外の定義 | 実のみ・preferred_areas NULL は数えない(false)・仮は NULL（#29と同一条件） | ☒ |
| モード記録 | dispatch_drivers.preferred_area_first に false/true が記録 | ☒ |
| 表示名ビュー | CA→area=AR-A/municipality=M-A/zone範囲1..3、CB→M-B/2..2（area_master由来・zone_plan不使用） | ☒ |
| 切替 | offices.preferred_area_first の true↔false で挙動と off_preference の符号が反転 | ☒ |

## 合格条件との対応
- offices.preferred_area_first（既定false）がある・false=現行と同じ割当（回帰一致） … ✅（回帰一致＋cap回帰）
- true=Phase1主担当に希望一致が最優先・希望未設定はフォールバック・突合はcommon_id … ✅（true=希望第一・フォールバック）
- 希望一致割当が増え希望外が減る・未割当は増えない・cap・仮・1=1は両モード共通 … ✅（希望外100→0・未割当0）
- 表示名ビューが common_id→area・municipality＋zone_no範囲（area_master由来・zone_plan不使用） … ✅（表示名ビュー）
- 分岐は dispatch_build 関数内（4呼出経路で一貫）・用語が用語集v0.1どおり … ✅（関数内分岐）

## 申し送り（実機・次担当）
- **実機適用順**: ④(migrate_functions) → cap_wire(shift_mgmt) → 本モジュール②。cap_wire を後から流すと #28 が巻き戻る（②は cap_wire 版を土台に転記）。
- **#29 は本モジュールを薄く消費**: 希望外件数＝`dispatch_assignments.off_preference` 集計、表示名＝`common_id_display`。希望外定義・表示名解決はここで確定＝#29 で別定義しない。
- 実機での回帰確認は、実データで false 実行 → 既存配車と割当一致を確認（pglite は決定的スタブ zone_rank による事前検証）。
