# 確認結果メモ（認証・権限 残課題 v1.1）

- 実施日: 2026 / 07 / 10
- 事前検証: Claude Code（pglite storage **26/26**・audit **9/9**）＋ Fable独立コード監査（②）
- 実機（ログイン・Storage API経路）は **業務B で確認** → `verify_rls_scope_checklist_v0.md` に記入

## 成果物
- `storage_rls_all_buckets_v0.sql`（① 3バケットを office prefix × my_* で制限・select/insert/update）
- `audit_address_master_v0.sql`（② 調査のみ・読むだけ。参照検出＋語彙ゲート）
- `verify_rls_scope_v0.sql`（③ なりすましで範囲外0件を機械証明）＋ `verify_rls_scope_checklist_v0.md`（実機サインオフ）
- `pglite_test_storage.mjs` / `pglite_test_audit.mjs`

## ① Storage 営業所別prefix制限 — pglite 26/26 PASS
| 観点 | 期待 | 判定 |
|---|---|---|
| 出荷SQLがそのまま適用できる（ヘルパ＋9ポリシー） | エラーなし | ☒ |
| area: 自営業所のみ読める／他営業所0件（3バケット） | 範囲内1件・範囲外0件 | ☒ |
| hq: 全office／depot: 配下のみ／driver: 0件 | それぞれ一致 | ☒ |
| 書込: area自営業所=可・他営業所=拒否・prefix無し=拒否 | 一致 | ☒ |
| depot書込: 配下=可・配下外=拒否 | 一致 | ☒ |
| upsert上書き（update）: 自営業所=可・他営業所=不可 | 一致 | ☒ |

## ② address_master — 結論: **今回は drop しない**（調査のみ納品）
- 指示書の条件「参照が無ければ drop」→ Fable独立監査で**参照3件を確定**（条件不成立）:
  `zone_rank`／`dispatch_build`（配車エンジン）／`delivery_status_public`（anon公開API・SECURITY DEFINER）
- pglite 9/9 PASS:
  - §1-1 prosrc検索で3関数を検出（pg_depend は0件＝「dropが素通りする」落とし穴を実証）
  - §2〜§4 の語彙ゲートが、仕込んだ旧語彙残置・未知隣接ID・municipality非一意を正しく検出
  - 監査SQLに DROP/DML/DDL が含まれない（読むだけ保証）
- 検証環境での audit 実行結果（業務Bで記入）:
  - §1-1 検出関数: ＿＿＿＿＿／§2 old_vocab_only: ＿＿＿／§3: ＿＿＿／§4: ＿＿＿

## ③ 範囲外0件 — SQLなりすまし＋実機の2段構え

### §1 area（IT01）— **実施済み・全23行 OK**（2026/07/10）
なりすまし確認: `role=area / office=IT01`（judge=OK）

| 観点 | 実測 | 判定 |
|---|---|---|
| deliveries 範囲内 / 範囲外 | 2548 / **0** | ☒ |
| drivers 範囲内 / 範囲外 | 9 / **0** | ☒ |
| work_schedules 範囲外 | **0** | ☒ |
| offices 範囲外 | **0** | ☒ |
| print_history 範囲外 | **0** | ☒ |
| delivery_index / delivery_status_log 範囲外 | **0 / 0** | ☒ |
| profiles 自分 / 他人 | 1 / **0** | ☒ |
| hq限定8表（area_master・zone_plan・renumber_plan・dispatch_*・shift_hours） | すべて **0** | ☒ |
| hq限定 area_master_staging | **GRANT無し(アクセス不可)** ＝0件より強い | ☒ |
| **storage 範囲内 / 範囲外（3バケット）** | **4 / 0** | ☒ |

→ **①のStorage営業所別prefix制限が本番DBで実証された**（pglite だけでなく実環境で範囲外0件）。
→ 「範囲外=0」と「範囲内>0」が対で成立（塞ぎすぎでもない）。

### §2 driver（DRV001）— **実施済み・全6行 OK**（2026/07/10）
なりすまし確認: `role=driver / driver_id=DRV001`（judge=OK）

| 観点 | 実測 | 判定 |
|---|---|---|
| deliveries 範囲内（自分の担当） | 241 | ☒ |
| deliveries 範囲外（他人の担当） | **0** | ☒ |
| work_schedules 範囲外 | **0** | ☒ |
| drivers 範囲外（自分以外） | **0** | ☒ |
| **storage 範囲外（3バケット全体）** | **0** ＝ドライバーは帳票を一切読めない | ☒ |

### §3 shipper（SHIP01）— **実施済み・全6行 OK**（2026/07/10）
なりすまし確認: `role=shipper / shipper_id=SHIP01`（judge=OK）

| 観点 | 実測 | 判定 |
|---|---|---|
| deliveries 範囲内（自荷主） | 27 | ☒ |
| deliveries 範囲外（他荷主） | **0** | ☒ |
| shippers 範囲外 | **0** | ☒ |
| drivers 範囲外（全件不可） | **0** ＝荷主はドライバー情報を一切見られない | ☒ |
| storage 範囲外（全件不可） | **0** ＝荷主は帳票を一切読めない | ☒ |

### 残り（業務Bで記入）
| ブロック | 主張（範囲外0件） | 対（範囲内>0） | 判定 |
|---|---|---|---|
| §4 depot | 配下外=0 | 配下>0 | ☐ |
| §5 hq | —（全件見える） | 各表>0 | ☐ |
| §6 書込 | 業務表への直write拒否（write policy 0本） | DEFINER経由は成功 | ☐ |
| 実機B-1〜B-4 | checklist 参照（Storage APIの実ダウンロード経路） | | ☐ |

※ §0 の一覧に該当ロールのユーザーが居ないブロックは「対象ユーザー無し」と明記して未実施とする（黙って飛ばさない）。

## 備考
- ①で **update ポリシーを追加**（フロントは `upsert:true`＝同日再保存が UPDATE になるため。従来は insert のみで上書き保存が失敗する潜在バグ）。
- 骨格（RLS ON・読取可視範囲・DEFINER書込・my_*）は無変更。
- ②の移行（3関数の area_master 書換→drop）は語彙ゲート合格後に**別指示書**で。
